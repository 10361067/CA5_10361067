#creating calculator with the same functions as in CA1

#addition function
add <- function(x, y) {
    return(x+y)
}
add(5,2)

#subtraction function
substract <- function(x, y) {
    return(x-y)
}
substract(9,7)

#multiplication function
multiply <- function(x, y) {
    return(x*y)
}
multiply(3,3)

#division function
divide <- function(x, y) {
    return(x/y)
}
divide(49,7)

#square function
squared <- function(x) {
  return(x^2)
}
squared(11)

#square root function
square_root <- function(x) {
    return(sqrt(x))
}
sqrt(225)

#factorial function
fact <- function(x) {
    return(factorial(x))
}
factorial(5)

#power function???
power <- function(x, y) {
    return(x^y)
}
power(3,5)

#exponential function
exponential <- function(x) {
    return(exp(x))
}
exp(5)

#natural logarithm function
logarithm <- function(x) {
    return(log(x))
}
log(10)

